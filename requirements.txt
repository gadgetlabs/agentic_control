# Robot brain dependencies
strands-agents                  # agentic framework (@tool + Agent loop)
strands-agents[litellm]         # bridges Strands tool-calling to Ollama

ollama                          # local LLM inference  (ollama pull qwen2.5:3b)
faster-whisper                  # Whisper STT
pyserial-asyncio                # async serial port
python-can                      # CAN bus (socketcan on Linux)
python-dotenv
piper-tts                       # neural text-to-speech (en_GB-jenny_dioco-medium voice)
sounddevice                     # audio playback for piper output
numpy                           # needed to convert raw PCM bytes for sounddevice

# Wake word - install from source:
# pip install git+https://github.com/gadgetlabs/simple-wake-word
